import styled from 'styled-components';

const Header = styled.h4`
  font-family: Didot, Georgia, Garamond, ‘Times New Roman’, Times, serif;
  font-weight: 600;
  padding: 0.25rem 0;
  font-style: normal;
  margin: 0;
`;

const UnorderedList = styled.ul`margin-bottom: 0rem;`;

export { Header, UnorderedList };
